# Gray-Scott Results Note

This experiment is retained with a performance caveat.

- Full sweeps at `N=32` can be very slow.
- Recommended workflow: coarse stage at low resolution, then refine selected `(F,k)` regimes.
